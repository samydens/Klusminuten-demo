<div>
    @switch($slide)
        @case(0)
            {{-- Include job component --}}
            @break
        @case(1)
            {{-- Include employee component --}}
            @break
        @case(2)
            {{-- Include client component --}}
            @break
    @endswitch
</div>
